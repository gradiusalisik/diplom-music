include /blocks/container/container
include /blocks/link-arrow/link-arrow
include /blocks/button/button
include /blocks/title/title
include /blocks/text/text
include /blocks/popup/popup

mixin kontrabas()
	+b.kontrabas&attributes(attributes)
		+container
			+e.wrapper
				+title.kontrabas__title КОНТРАБАС

				+text._size_small-big.kontrabas__text
					| Инструмент басового и тенорового регистра, известный с первой половины XVI века.

				+text._size_small-big.kontrabas__text
					| Звук сочный, певучий и напряжённый, в верхнем регистре слегка сдавленный. Такого же строения, что и скрипка, однако значительно больших размеров. На виолончели играют сидя, ставя её перед собой и упирая в пол особой ножкой с остриём

			+e('img').image(src='assets/images/instruments/kontrabas.png')

			+e.footer
				+link-arrow({href: 'stringed-bow.html'})

				+e.wrap-buttons
					+e.button-video
						+button({type: 'video', popup: 'kontrabas'})
					+button({audio: 'assets/audio/instruments/contra.mp3'})

		+popup({id: 'kontrabas'})
